server:
  port: 8080

spring:
  datasource:
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3309/curtis_asia?serverTimezone=UTC
      username: root
      password: 233333

mybatis-plus:
  global-config:
    db-config:
      table-prefix: curtis_
      id-type: auto
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
